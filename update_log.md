## 更新日志

### 2.0.0

> 这是一次大型更新：重构云同步架构与 AI 模型体系，新增完整图片模块，并带来大量交互与体验优化

> **⚠️ 重要提示（不可降级）**：本次更新将数据库结构升级至 v16 —— 书籍 / Mod 主键由自增 id 改为 UUID，并新增图片相关列。**升级 2.0.0 后产生的数据库无法在 1.x 旧版本上运行**：旧版本无法打开或读取该数据库，可能报错甚至损坏数据。请勿降级回 1.x 使用 2.0.0 的数据；若需在旧版本环境继续使用，请务必在升级前另行备份旧版数据。

- 云同步架构重构
    - 数据库与图片两平面解耦，书籍等对象主键全面引入 UUID（书籍主键由自增 id 改为 uuid，数据库升级至 v16），跨设备合并更稳健
    - 图片删除改为全局墓碑机制，撤销升级为带时间戳的全局复活标记，修复跨设备复活被反杀的问题
    - 自动同步改为用户操作节点驱动，移除空闲定时轮询；回到前台静默同步带节流窗口
    - 云端单侧设置与 Mod 变更不再自动拉取，改走人工确认通道；轮次仅纯增量自动合并
    - 同步结果提示改为应用级悬浮气泡：成功/取消类自动消失、失败类驻留可复制，文案统一为「云端记录 #N」
    - 同步落地后自动刷新书籍与设置页字段，避免陈旧数据；编辑内容保存后刷新时间戳并触发自动云同步
    - 「保留历史版本份数」设置迁移至云端 sync_config.json 唯一持有
- 数据合并与管理
    - 新增「数据库合并」决策页：逐书展示冲突、仅单侧有、全一致状态，支持按最后时间、轮次、全部导入/全部本地自动勾选，合并前附变更摘要确认
    - 新增数据库导入冲突合并；Mod 冲突卡片并排展示「导入的备份」与「本地」对比预览，点击选择保留侧
    - 合并书籍时按设置修改时间决定设置部件默认取舍，持平或未知默认保留导入侧
    - 新增「数据库结构」调试页，只读查看数据表结构、索引与内容
    - 修复 Android 上数据库导入文件选择器无法弹出的问题
- 模型体系重构
    - 接入配置重构为「平台 + 模型」组织，预置 DeepSeek 开放平台（V4 Pro / V4 Flash）
    - 模型能力独立可配：流式、思考、联网按模型自由开关，内置预置模型能力固定
    - 新增模型识图能力开关与 Vision Exp 预置模型
    - Chat 输入栏新增模型选择器，支持快速切换当前对话模型
    - 修复配置异常导致内置默认平台/模型可被删除的问题
- 图片模块（新增）
    - 支持对话图片存储与导入，新增图片备份与单张大小上限（默认 16MB）；可选将导入的 .jpg 自动转存为 .jpeg
    - 图片查看器重做：多图左右滑动切换、双指/滚轮缩放、页码指示器、单击/双击缩放与单击退出
    - Windows 端支持独立窗口打开图片查看器，主窗口预热常驻、开图复用秒开
    - 查看器新增长按/右键菜单：另存为、复制、删除（删除前二次确认）；新增触屏下滑关闭手势
    - 新增跨平台剪贴板图片写入服务（Windows 原始文件与 PNG/CF_DIBV5/CF_DIB 多格式）
    - 子查看器窗口删除图片即时同步到图片库
    - 修复 Android 图片另存为与剪贴板复制；修复删除角标被图片遮挡、拖动/缩放手势冲突漂移、缩放越界、窗口尺寸变化不适配等问题
- 聊天与输入体验
    - 输入框支持 Ctrl+V 与右键菜单粘贴文本与图片（图片按识图门控加入待发送附件）
    - 修复 Windows 11 下 Win+V 剪贴板历史粘贴失灵的问题
    - 支持 Ctrl+Enter 快捷发送
    - 失败条目持久化用户消息图片，重试/修改重问时自动复用原图
    - 编辑组件支持取消并丢弃修改，保存/取消由外部统一驱动；保存后刷新时间戳
    - 新增快速定位滚动导轨：边缘导轨 + 目录浮层展开动画
    - 宽屏 Chat 页右侧栏支持拖拽调整宽度并持久化，双击分隔线恢复默认
    - 修复懒加载消息列表滚动不到真实底部的问题
- 设置与界面
    - 新增「存储管理」面板，支持查看、导出与清理本地数据
    - 设置页窄屏内容区支持左右滑动切换子页面（鼠标、触控板亦可拖拽翻页）
    - 主题新增 success 颜色；修正「API设置」与同步面板等文案
- Mod
    - 点击 Mod 条目打开只读预览对话框
    - 修复预置 Mod 保存时外键冲突的问题
- 后台与通知
    - Windows 任务栏闪烁提醒后台生成完成，窗口回到前台自动停止
    - 新增启动自动检查更新：「关于」面板可开关（默认开启），24h 节流、支持跳过指定版本，失败经系统通知提示且不影响启动
- 其他
    - 修复旧版插件 compileSdk 过低导致 AAR 元数据校验失败；构建脚本新增转圈指示器与无 stdin 兜底
    - desktop_drop 升级至 0.8.0

### 1.3.1

- 修复本地设置项 Android 下未知的异常失效的bug
- 修复设置保存提示在部分未设置下产生错误的失败提示的问题
- 新增每轮使用模型展示
- 简化预置 deepseek v4 ga 思考链管理模组
- **重点** 将 flutter 版本由 3.44.8 升级至 `3.47.0`
- 修复 Windows 端旧数据库无法读取：数据库迁移改为幂等（若 `rounds` 已存在 `model_name` 列则跳过 ALTER，避免 `duplicate column name` 导致库无法打开）

### 1.3.0

> 这是一次大型更新，新增、优化、重构了多个功能模块与界面

- 增加 Agent 支持
    - 增加联网搜索支持并进行完善
- 重构聊天页面
    - 重构流式过程，优化思考模式展示，支持详细的 Agent 调用记录
    - 重构输入区，迁移流式、思考开关
    - 重构操作部件，统一 UI
    - 重构`调试`功能为`RAW`功能，更优雅地支持 Agent 和调试查看、支持转译换行符
    - 增加对话楼层跳转悬浮条 (BETA)
    - 聊天气泡支持 Markdown 渲染
- 重构设置界面
    - 重构 AI 模型选择页面
    - 重构保存逻辑
    - 增加开放源代码许可页
    - 增加更新日志页
    - 优化关于页面
- 重构部分基础功能
    - 重构模型配置和预设系统，支持自定义模型
    - 重构生成连接模块，修复断流，增加自动重连，支持多书并发和页面内提示
    - 重构 Markdown 渲染模块且进行模块化重构，优化视觉体验
- 新增后台生成通知设置
    - 增加通知方式 Android 端后台生成保活
    - 增加 Windows 和 Android 端的后台任务完成系统推送功能
    - 增加检测 Android 端通知是否开启的功能
- 优化主界面的书籍列表标题显示
- 新增尝试对思维链进行优化以提升 AI 性能的模组

### 1.2.1

- 支持输入框 md 语法高亮
- 优化开局预设提示词
- 优化对话页 UX
- 修复设置页导航项 UI

### 1.2.0

- 修改主页为书籍列表
- 优化新建书籍体验，支持新建时全设置
- 优化云同步操作体验
- 支持书籍、mod模糊搜索
- 优化提示词
- 优化构建

### 1.1.4

- 发布构建迁移至 Dart 脚本（`tool/build_release.dart`），支持 Windows 安装包（setup.exe）生成。
- 点击输入框外部时自动收起键盘。
- 优化网文预设提示词。

### 1.1.3

- 增强 AI 响应解析容错：剧情演绎缺失时依次回退「正文」区块与响应开头内容作为正文。
- 完善流式生成超时控制：新增空闲超时与 100ms 轮询取消，挂起时可立即响应中断。
- 修复非流式生成停止后需等待生成完成才生效的问题。
- 失败处理重构为书籍级失败条目并升级数据库；品牌 Logo 改用圆角应用图标。

### 1.1.2

- 新增图标生成脚本并更新 Android / Windows 应用图标（Windows 圆角处理）。
- 支持通过 `key.properties` 配置 release 正式签名（未配置时回退 debug 签名）。
- 移动端抽屉支持左滑打开与右滑关闭；打开时系统返回键优先关闭抽屉。

### 1.1.1

- 移除本轮临时前置词 / 后置词功能。
- 用户滚动阅读历史时暂停自动跟随，避免流式输出拽回底部。
- 裁剪抽屉阴影，避免收起后投射到屏幕边缘。

### 1.1.0

- 新增默认「好孩子」预设与高强度「去 AI 味」预设，优化网文语感预设提示词。
- 精简默认角色层级与角色状态模板。
- 发送中防重复提交，临时指令仅生效一次并避免重复写库。
- 修复 Windows 长文本编辑框滚动后输入法候选窗不跟随光标的问题。
- 性能与重构：抽取聊天界面常量、拆分宽窄屏布局、统一 ResponsiveBuilder、大规模清理冗余代码。

### 1.0.2

- 优化 Mod 面板与主题模式设置在窄屏 / 宽屏下的布局。
- 修复长按气泡时滑动列表误弹菜单的问题。
- Android 包名更新为 `com.yueshix.narrchat`。

### 1.0.1
- 初始 git 记录
    - 以下内容包括之前的部分
- 新增暗色模式支持（跟随系统 / 亮色 / 暗色）。
- 新增 WebDAV 云同步：数据库备份与恢复（替换 / 合并），支持自动上传。
- 新增 Mod 功能：自定义提示词内容包、预置 Mod、与书籍关联。
- 新增全局字体设置与系统字体扫描加载。
- 重构数据存储：用户数据（sqlite，可云同步）/ 本地数据（JSON）/ 令牌安全存储分离。
- 新增版本号统一管理（release.yaml）与发布构建脚本。
- 设置页重构为全窗口设置；世界书迁入书籍设置面板。
- 统一记忆总结格式并强化提示词约束；提示词中注入上轮时间。
- 新增 AI 生成中断功能；生成期间显示用户未落库消息气泡。
- 调试对话框支持按字符搜索并高亮定位。
- 完善提示词规则；新增 Markdown 波浪线转义；启动时窗口居中显示。
